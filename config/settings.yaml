database:
  path: "data/db/ci_monitor.sqlite"

collectors:
  ares:
    schedule: "weekly"
  job_postings:
    schedule: "daily"
  news:
    schedule: "hourly"

logging:
  level: "INFO"
  file: "data/ci_monitor.log"
